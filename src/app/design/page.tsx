/**
 * Design Studio Page
 *
 * Main page for the design studio at route /design.
 * Provides UI infrastructure for managing and editing diagrams,
 * documents, images, and other design content.
 */

import { DesignStudioView } from "@/views/design-studio/design-studio-view";

export default function DesignPage() {
  return <DesignStudioView />;
}
